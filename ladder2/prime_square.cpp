#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define y "yes"
#define n "NO"


int main(){
    ll t;
    cin >> t;
    while(t--){
        int a;
        cin >> a;
        int table[a][a];
int* begin = &table[0][0];
size_t size = sizeof(table) / sizeof(table[0][0]);
fill(begin, begin + size, 100);
for (int i = 0;i<)
    }
}
