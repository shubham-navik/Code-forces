#include<bits/stdc++.h>
using namespace std;
#define ll long long


int main(){
    ll n;
    cin >> n;
    ll sum = 0;
    while(n--){
        ll a;
        cin >> a;
        sum += (a);
    }
    cout << abs(sum) << endl;
    return 0;
}